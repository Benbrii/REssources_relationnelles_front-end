import React from 'react';
import Navigation from '../Components/Navigation';

const Groupe = () => {
    return (
        <div>
            Ici c'est les petits pedestres
            <Navigation/>
        </div>
    );
};

export default Groupe;