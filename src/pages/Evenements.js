import React from 'react';
import Navigation from '../Components/Navigation';

const Evenement = () => {
    return (
        <div>
            Hello depuis evenement
            <Navigation/>
        </div>
    );
};

export default Evenement;